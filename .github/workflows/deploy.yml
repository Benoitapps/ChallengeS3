on: 
  push:
    branches:
      - feature/ci-cd

name: Publish Website
jobs:
  web-deploy:
    name: 🚀 Deploy Website
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get Latest Code
      uses: actions/checkout@v3
    
    - name: 📂 Sync Files
      uses: SamKirkland/web-deploy@v1
      with:
        target-server: 5.196.23.210
        remote-user: debian
        private-ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
        destination-path: ~/lab/challengeS3